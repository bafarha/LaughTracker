<div class="main tag-index">
	<div class="container">
		<div class="row">
			<% @tags.each do |t| %>
			<div class="tag col-lg-2">
				<h3><%= t.title %></h3>
				<%= link_to "See Tag", tag_path(t) %>
			</div>
			<% end %>
		</div>
	</div>
</div>
