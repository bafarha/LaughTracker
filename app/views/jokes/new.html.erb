<div class='main'>
	<div class='container'>
		<%= form_for (@joke) do |f| %>
			<div class="field">
				<%= f.label :new_joke %><br>
				<%= f.text_area :title %>
				<%= f.text_area :content %>
			</div>
			<div class="actions">
				<%= f.submit "create" %>
			</div>
		<% end %>
	</div>
</div>