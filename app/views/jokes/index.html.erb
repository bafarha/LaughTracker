<div class="main">
	<div class="container">
		<h1>My Jokes</h1>
		<% @jokes.each do |j| %>
			<div class="jokes">
				<h3><%= j.title %></h3>
				<small><em><%= "#{time_ago_in_words(j.created_at)} ago" %></em></small>
				<p><%= j.content %></p>
			</div>
		<% end %>
	</div>
</div>

